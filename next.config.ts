// next.config.js
const nextConfig = {
  images: {
    domains: [
      'a.storyblok.com', 
      'www.intoxianime.com',  // Adicione este domínio que está no seu PostCard
      // Adicione outros domínios de imagens que você utiliza
    ],
  },
};

module.exports = nextConfig;